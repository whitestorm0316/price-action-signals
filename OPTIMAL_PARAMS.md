# 「最优参数」是什么？

> **问题**：最优参数是什么？
> **直接答案**：**不存在可交易的「最优参数」。** 但有**一个维度**（止损宽度）确实单调改善绩效，
> 以及**一组「最接近可验证」的落点**（`--preset trend_wide`）。两者都不是「最优」，理由如下。
> 数据：ETH / BTC-USDT-SWAP 永续，3 年 4H 共 6,570 根，真实 OKX 历史 K 线。
> 复现：`OKX_PROXY=http://127.0.0.1:7897 .venv/bin/python probe_pa_stopwidth.py`

---

## 一、为什么「最优参数」不存在：三层逐一否证

### 层 1 · 信号层（用什么入场、什么过滤）—— **没有最优，只有噪音**

| 检验 | 结果 | 含义 |
|---|---|---|
| 4H 方差比 VR | ETH 1.036（z=+1.03）/ BTC 0.983（z=−0.44） | 价格近似**鞅**，无方向记忆 |
| 形态 α（匹配随机入场对照 200 次） | ETH **−0.0065R**（p=0.845）/ BTC **−0.0065R**（p=0.815） | 形态**不含方向信息** |
| 纯趋势 α（MA20 斜率，无形态） | ETH −0.0618R（z=−0.79）/ BTC −0.0188R（z=−0.22） | 趋势择时**也不显著** |
| 所有间隔的 t 值（g=4~40） | 全部 **< 2**，Bootstrap CI **全部含 0** | 效应量本身在噪音范围内 |

**结论**：在入场信号这一层调参（周期、均线类型、量能过滤、间隔、盈亏比）**没有最优解**——
是在噪音里挑最大值。ETH 三年正期望 +0.161R（t=1.84）看着不错，但 **1,302 笔样本也只有 t=1.19**，
说明不是"数据不够"，而是**效应量为零**。

### 层 2 · 止损宽度层（× ATR）—— **有单调改善，但 argmax 不可用**

这是价格行为学框架里**唯一被数据支持**的实变量。实测净期望（扣 0.070% 往返费）：

| k×ATR | ETH 形态净 | ETH 随机净 | BTC 形态净 | BTC 随机净 |
|---|---|---|---|---|
| 0.50 | −0.0914 | −0.1099 | −0.1641 | −0.1661 |
| 0.75 | −0.0443 | −0.0599 | −0.0851 | −0.0750 |
| 1.00 | +0.0115 | −0.0246 | −0.0468 | −0.0342 |
| 1.25 | +0.0141 | +0.0037 | −0.0069 | −0.0055 |
| **1.50** | **+0.0182**（t=+0.60） | +0.0036 | **+0.0372**（t=+1.22） | +0.0216 |
| **2.00** | +0.0172（t=+0.57） | +0.0074 | **+0.0550**（t=+1.81）← BTC 峰值 | +0.0307 |
| 2.50 | +0.0471 | +0.0258 | −0.0114 | −0.0073 |
| **3.00** | **+0.0833**（t=+2.76）← ETH 峰值 | +0.0389 | −0.0517（t=−1.77） | −0.0361 |

**这张表否证了「存在唯一最优 k」的假设，四条独立理由：**

1. **两品种 argmax 不一致**：ETH 峰值 k=3.0、BTC 峰值 k=2.0。
   同一套规则在两个品种上的最优值不同 ⇒ 挑出来的 k 是**样本特性**，不是市场规律。
2. **ETH 的峰值落在网格边界**（k=3.0 是扫描上限）。项目自身方法论（闸门 5 子检查）明确警告：
   *argmax 总落在网格最大值 = 最优在测试范围之外 = 该点估计不可信*。若把网格延到 k=4、5，
   ETH 曲线还会继续升（因为 f 还在降）。
3. **形态源与随机源曲线形状一致**（ETH 两者同升；BTC 两者同在 2.0 见顶、3.0 转负）。
   形状一致 ⇒ 这个效应**完全不依赖入场信号**，是止损宽度本身的性质。
4. **毛利不是主因**：ETH 净跨度 +0.1747R 里毛利贡献 +0.1066R（61%），
   但 **BTC 净跨度 +0.1124R 里毛利只贡献 +0.0153R（14%）**。
   ⇒ 「宽止损」的收益本质是**少交手续费**（f 从 0.109 降到 0.018），不是"看对方向"。

**因此**：k 越大越好这件事**在任何一个具体数值上都不显著**（同宽度择时 α：ETH |z|max 1.45、
BTC |z|max 0.79，全部不显著）。它是**单调的成本改善**，不是**可择优的参数**。

**正确用法**：把 k 当作「**成本旋钮**」而不是「**收益旋钮**」——
报**区间**而不是**峰**：

| 品种 | 建议区间 | 理由 |
|---|---|---|
| ETH | k ∈ [1.5, 2.5] | 曲线单调但**无内点峰**，只在边界外更高 → 只能说"越大越好"，不能说"最优在 3.0" |
| BTC | k ∈ [1.5, 2.0] | 有内点峰（2.0），但 2.5 起转负 → 上限有市场依据 |

**必须同时盯一个副作用指标**：`超时率`。k=3.0 时超时率升到 3.5%（k=1.5 时仅 0.1%）——
「止损放宽」的收益是靠「**持仓变长、单笔占用时间增加**」换来的，不是免费的。
`k=1.5` 因此在两品种上都落在区间的**保守端**（不是最优端）。

### 层 3 · 成本层 —— 真正有效的动作

| 动作 | 效果 | 性质 |
|---|---|---|
| 周期 15m → 4H | f：0.18R → 0.041R | 真实机制，可复用 |
| 突破入场（替代收盘价） | 胜率 34% → 41% | 真实机制，但**需触发单**，本项目尚未支持 |
| 大幅降频（同形态间隔 ≥ 40） | 减少追单 | 真实机制 |
| 止损放宽到 1.5~2×ATR | f 再降一半 | 真实机制 |

这四条是**唯一可复用的东西**，但它们的本质是**降成本**，不是**创造优势**。

---

## 二、如果一定要给一组参数：`--preset trend_wide`

```bash
./.venv/bin/python main.py --preset trend_wide --backtest 300
# = --signal-mode trend --trend 20 --atr-mult 1.5 --bar 4H --rr 2 --inst BTC-USDT-SWAP
```

它是**价格行为学框架内最接近"可验证"的一个落点**：

| | n | 净期望 | t | 同向随机 | **趋势 α** | z |
|---|---|---|---|---|---|---|
| ETH | 4,840 | −0.0137R | −0.67 | +0.0481R | −0.0618R | −0.79 |
| BTC | 4,860 | **+0.0398R** | **+1.93** | +0.0587R | −0.0188R | −0.22 |

**但三重警告仍然成立：**

1. **未通过独立验证**（`STRATEGY_CONCLUSIONS.md`：*「没有任何配置通过独立复核。不要投入资金。」*）。
2. **BTC 的正 t 来自市场漂移**：α 只有 −0.0188R（z=−0.22）——
   **t=+1.93 是"BTC 这三年涨得更好"的副产品，不是趋势择时的功劳。**
   （与 `DIRECTION_REPORT.md` 里 TSMOM 的 t=1.70 是同一现象。）
3. **ETH 上为负**（−0.0137R），两品种不一致。

> **它是"诚实展示"用的，不是"推荐交易"用的。**
> 已在 `strategy_config.py` 中标注为**阻断类告警**：用它启动真金自动交易需显式 `confirm_unvalidated=true`。

---

## 三、唯一有结构性正期望的方向：资金费 Carry

如果问题放宽到"当下有什么好策略"，答案是**低频 delta 中性 carry**（现货多 + 永续空）：

- 3.65 年实测：ETH 毛费率年化 **+7.29%**、BTC **+7.23%**，正比例 85.8% / 86.3%，13 个季度无一为负。
- 优势来自**合约机制**（永续空头机械收资金费），不来自价格预测 ⇒ **不受"样本墙"约束**。
- 修正真实成本（0.20%~0.30% 双腿开平）后：**只有 ≥30 天换仓才成立**，
  净年化 **4.85%（30天）~ 6.45%（90天）**，最大回撤 −0.4%~−1.1%。
- **天花板**：现货腿必须全额付款 ⇒ **总名义永远 < 1 倍本金，无法加杠杆**；
  借 USDT 的日利率 0.02~0.03% = 年化 7.3~11%，**已高于 carry 毛收益**。
- 对照 USDT 活期理财 4.80%：30 天换仓超额 **+0.02pp（等于没有）**，90 天 **+1.65pp**。

---

## 四、一句话总结

> **信号层没有最优参数（α≈0，全是噪音）；
> 止损层有单调改善但没有可择优的唯一值（argmax 不一致 + ETH 落在网格边界 + 效应机械）；
> `trend_wide` 是最接近可验证的落点但未通过复核；真正有正期望的是 carry，但它跑不赢理财多少。**
>
> 详见 [`STRATEGY_CONCLUSIONS.md`](STRATEGY_CONCLUSIONS.md)、
> [`PRICE_ACTION_SYNTHESIS.md`](PRICE_ACTION_SYNTHESIS.md)、[`CARRY_REPORT.md`](CARRY_REPORT.md)。
